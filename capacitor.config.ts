import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.rpls.jerusalemcompass',
  appName: 'House of El, Compass',
  webDir: 'www'
};

export default config;
